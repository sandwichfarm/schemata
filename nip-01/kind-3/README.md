# NIP-01/kind-3

```
yarn install
yarn build:test

#build a bunch of (useless) serializers ... tldr: most juicy data is in tuples, and quicktype codegen doesn't **appear** to support tuples.
yarn build:all
```
